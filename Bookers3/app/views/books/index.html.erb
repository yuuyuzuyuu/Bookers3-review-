<%= render 'users/info', user: current_user %>

<%= render 'books/new', book: @book %>

<h2>Books</h2>
  <table>
    <theader>
      <tr>
        <th></th>
        <th>Title</th>
        <th>Opinion</th>
      </tr>
    </theader>
    <tbody>
      <% @books.each do |book| %>
        <tr>
          <td></td>
          <td>
            <%= link_to book_path(book.id) do %>
              <%= book.title %>
            <% end %>
          </td>
          <td><%= book.body %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
      